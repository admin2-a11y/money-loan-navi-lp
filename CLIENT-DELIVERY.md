# マネーローンナビLP 納品ファイル

納品区分：離脱防止あり

## 公開用入口

- `index.html`：アンケートから診断結果まで動作する完全版
- `result.html`：アンケート回答に応じて表示を切り替える通常の診断結果ページ
- `result-type2.html`：簡略表示版の診断結果ページ

## クライアント確認用の会社別ページ

- `result-mobit.html`：SMBCモビットを固定表示
- `result-promise.html`：プロミスを固定表示
- `result-aiful.html`：アイフルを固定表示
- `result-acom.html`：アコムを固定表示

会社別ページは確認用途のため、URLに別の回答値が含まれても対象会社を固定表示します。公開時の通常導線には `index.html` と `result.html` を使用してください。

## 離脱防止の動作

- スマートフォン：TOPページで戻る操作をした場合に1セッション1回表示
- PC：TOPページまたは診断結果ページで、マウスが画面上端から外れた場合に1セッション1回表示
- 診断結果ページからTOPページへ戻るスマートフォンの通常導線は妨げません
- 商品CTAを操作した後は表示しません
- 表示・クリック・閉じる操作は `dataLayer` へイベントを送ります

## 設置方法

ZIPを展開し、中のファイルとフォルダ構成を変えずにWebサーバーへアップロードしてください。動作確認は `file://` で直接開かず、HTTPまたはHTTPS環境で行ってください。

外部計測を利用する場合は、`DELIVERY-OPERATIONS.md` の同意管理・セキュリティ設定も確認してください。
